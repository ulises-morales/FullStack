<h1>Blogs</h1>
<% blogs.forEach(function(blog) { %>
  <div class="">
    <h2><%= blog.title %></h2>
    <img src="<%= blog.image %>" alt="">
    <span><%= blog.create %></span>
    <p><%= blog.body %></p>
  </div>
<% }); %>
