/*

Given array, index, and additional value,
insert the value into array at given index.
Do this without using built-in array methods.

*/

function indexAt(arr, index, value){
  arr[arr.length] = value;
  
}
